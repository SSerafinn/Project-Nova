OPENAI_API_KEY=your_openai_api_key_here
PORT=3001

# --- Local dev only ---
# CLIENT_URL is not needed on Railway (frontend is served from the same Express app)
# CLIENT_URL=http://localhost:5173

# --- Railway only ---
# Set DB_PATH to your mounted volume path, e.g. /data/notequest.db
# DB_PATH=/data/notequest.db
